clear
clc
close all hidden

file_id = fopen('abc_sample.dat');
A = fread(file_id, [number_of_abc_samples 5], 'float64');
% B = fread(file_id, sizeA, precision);
% 
% number_of_abc_samples = numel(A) / 5;
% data = reshape(A,  [number_of_abc_samples, 5]);
% clear A


fclose(file_id);